%start
  %h1= "Select a topic to get recommended some plates :)"
  %table
    - @all_nodes.each do |node|
      %tr
        %td= node["name"]
        %td
          = link_to "Choose", recommendations_v1_view_path(:node => node["id"])